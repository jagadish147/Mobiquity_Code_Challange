<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="viewModel"
            type="com.jagadish.mobiquitywetherapp.ui.home.BookmarksItemViewModel" />
    </data>

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:backgroundTint="@{viewModel.item.getColor()}"
        android:fitsSystemWindows="true"
        app:cardCornerRadius="24dp"
        app:cardElevation="8dp"
        tools:visibility="invisible"
        android:layout_marginBottom="10dp"
        android:layout_marginStart="5dp"
        android:layout_marginEnd="5dp">

        <RelativeLayout
            android:id="@+id/rootView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
            <TextView
                android:id="@+id/textViewCity"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerHorizontal="true"
                android:layout_marginTop="10dp"
                android:layout_marginBottom="24dp"
                android:text="@{viewModel.item.name}"
                android:textColor="@color/white"
                android:textSize="27sp"
                tools:text="35°" />
            <ImageView
                android:id="@+id/imageViewWeatherIcon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_marginStart="36dp"
                android:layout_marginTop="48dp"
                app:setWeatherIcon="@{viewModel.item.getCurrentWeather().icon}"
                tools:src="@drawable/a10d_svg" />

            <TextView
                android:id="@+id/textViewTemperature"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_marginTop="48dp"
                android:layout_marginEnd="36dp"
                android:text="@{viewModel.item.main.getTempString()}"
                android:textColor="@color/white"
                android:textSize="24sp"
                tools:text="25°" />

            <TextView
                android:id="@+id/textViewWeatherMain"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_below="@id/textViewTemperature"
                android:layout_centerHorizontal="true"
                android:layout_marginTop="8dp"
                android:text="@{viewModel.item.getCurrentWeather().main}"
                android:textColor="@color/white"
                android:textSize="23sp"
                tools:text="Clouds &amp; Sun" />

            <TextView
                android:id="@+id/textViewHumidityHeader"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_below="@id/textViewWeatherMain"
                android:layout_centerHorizontal="true"
                android:layout_marginTop="16dp"
                android:text="@string/humidity"
                android:textColor="@color/white"
                android:textSize="19sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/textViewHumidity"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_below="@id/textViewHumidityHeader"
                android:layout_centerHorizontal="true"
                android:text="@{viewModel.item.main.getHumidityString()}"
                android:textColor="@color/whiteWithOpacity"
                android:textSize="27sp"
                tools:text="35°" />


        </RelativeLayout>
    </com.google.android.material.card.MaterialCardView>
</layout>
